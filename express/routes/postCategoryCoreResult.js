const { PublicController } = require('../controllers/index')

module.exports = {
  // the express handler implementation for postCategoryCoreResult handler.
  postCategoryCoreResult: PublicController.postCategoryCoreResult
}
