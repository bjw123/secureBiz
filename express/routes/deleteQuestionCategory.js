const { AdminsController } = require('../controllers/index')

module.exports = {
  // the express handler implementation for deleteQuestionCategory handler.
  deleteQuestionCategory: AdminsController.deleteQuestionCategory
}
