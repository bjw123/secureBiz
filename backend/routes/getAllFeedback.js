const { AdminsController } = require('../controllers/index')

module.exports = {
  // the express handler implementation for getAllFeedback handler.
  getAllFeedback: AdminsController.getAllFeedback
}
