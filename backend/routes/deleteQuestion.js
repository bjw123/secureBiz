const { AdminsController } = require('../controllers/index')

module.exports = {
  // the express handler implementation for deleteQuestion handler.
  deleteQuestion: AdminsController.deleteQuestion
}
