const { AdminsController } = require('../controllers/index')

module.exports = {
  // the express handler implementation for addQuestionCategory handler.
  addQuestionCategory: AdminsController.addQuestionCategory
}
