const { AdminsController } = require('../controllers/index')

module.exports = {
  // the express handler implementation for updateQuestion handler.
  updateQuestion: AdminsController.updateQuestion
}
